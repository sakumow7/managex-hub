# Local portfolio mode. Set DEMO_ENABLED=true in backend/.env as well.
VITE_DEMO_MODE=true
# Leave /api locally (Vite/Nginx proxy). On Render, use the full backend URL:
# VITE_API_BASE_URL=https://YOUR-API.onrender.com/api
VITE_API_BASE_URL=/api
